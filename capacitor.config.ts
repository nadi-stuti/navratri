import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.nulligma.fansting_navratri",
  appName: "Fasting recipes and navratri guide",
  webDir: "dist",
};

export default config;
